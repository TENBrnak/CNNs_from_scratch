from image import Image

fn read_data_as_images(directory: String) raises -> String:
    return "VariadicList[Image]()"
    """
    if directory.exists():
        let data = directory.read_bytes()
        for i in range(100):
            print(data[i])
        return VariadicList[Image]()
    raise 'Directory does not exist.'
    """





